// ---------- src/ui/games/index.tsx ----------
import { createRoot } from "react-dom/client";
import FlashCards from "./FlashCards";

createRoot(document.getElementById("root")!).render(<FlashCards />);